//If else simple C program

#include <stdio.h>

void main(){

	if(0)
		printf( "0 must mean true.\n");
	else
		printf("1 must mean true.\n");
	
	if(-15)
		printf( "0 must mean false.\n" );
	else
		printf( "1 must mean false.\n" );

}
